{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "Bash",
        "hooks": [
          {
            "type": "command",
            "if": "Bash(git commit*)",
            "command": "pnpm fix && pnpm lint && pnpm test || echo '{\"continue\": false, \"stopReason\": \"pnpm fix/lint/test failed — fix errors before committing\"}'",
            "statusMessage": "Running fix, lint, test..."
          }
        ]
      }
    ]
  }
}
